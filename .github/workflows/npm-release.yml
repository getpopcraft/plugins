# Publish @popcraft/* to npmjs when a release is promoted from prerelease.
#
# AUTH: OIDC trusted publishing — no NPM_TOKEN. One-time setup on npmjs.com, per package
# (@popcraft/plugin-typings and @popcraft/plugin-examples), after the first manual publish:
#   Organization or user: getpopcraft
#   Repository:           plugin-examples
#   Workflow filename:    npm-release.yml
#   Environment:          (leave blank)
# Requires npm >= 11.5.1 and id-token: write.

name: NPM release

on:
  release:
    types: [published, edited]

jobs:
  publish:
    name: Publish packages
    # Promote path: a full release (not a draft, not a prerelease).
    if: github.event.release.draft == false && github.event.release.prerelease == false
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        package:
          - { dir: packages/plugin-typings, name: "@popcraft/plugin-typings" }
          - { dir: packages/examples, name: "@popcraft/plugin-examples" }
    steps:
      - name: Checkout the tag
        uses: actions/checkout@v7
        with:
          ref: refs/tags/${{ github.event.release.tag_name }}

      - uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          # Do NOT set registry-url — it injects a dummy NODE_AUTH_TOKEN that
          # breaks npm OIDC trusted publishing (E404 on PUT).

      - name: Upgrade npm for OIDC trusted publishing (needs >= 11.5.1)
        run: |
          npm install -g "npm@^11"
          npm --version

      - name: Skip if this version is already on npm
        id: gate
        run: |
          VERSION=$(node -p "require('./${{ matrix.package.dir }}/package.json').version")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          if npm view "${{ matrix.package.name }}@$VERSION" version >/dev/null 2>&1; then
            echo "published=true" >> "$GITHUB_OUTPUT"
            echo "${{ matrix.package.name }}@$VERSION is already on npm — nothing to do."
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Pack with pnpm
        if: steps.gate.outputs.published == 'false'
        # pnpm, not npm: only pnpm rewrites `workspace:*` specs to real versions.
        run: pnpm --filter "./${{ matrix.package.dir }}" pack --pack-destination "$RUNNER_TEMP"

      - name: Publish to npmjs
        if: steps.gate.outputs.published == 'false'
        env:
          # Ensure token auth cannot shadow OIDC (setup-node / secrets leftovers).
          NODE_AUTH_TOKEN: ""
          NPM_TOKEN: ""
        run: |
          set -euo pipefail
          if [ -z "${ACTIONS_ID_TOKEN_REQUEST_URL:-}" ] && [ -z "${ACTIONS_ID_TOKEN_REQUEST_TOKEN:-}" ]; then
            echo "::error::No GitHub OIDC token endpoint — check permissions.id-token: write"
            exit 1
          fi
          TARBALL=$(ls "$RUNNER_TEMP"/*.tgz)
          # Provenance is on: unlike the tish tarball flow, this tarball is built in this job
          # from this tag, which is exactly the case provenance attests. Set
          # NPM_CONFIG_PROVENANCE=false here if npm ever rejects it.
          npm publish "$TARBALL" --access public --provenance --registry https://registry.npmjs.org/
